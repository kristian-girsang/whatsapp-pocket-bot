services:
  bot:
    build: .
    container_name: whatsapp-ai-expense-tracker
    env_file:
      - .env
    ports:
      - "3001:3000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
